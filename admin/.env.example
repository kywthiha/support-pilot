DJANGO_SECRET_KEY="django-insecure-change-me-in-production"
DJANGO_DEBUG="True"
ALLOWED_HOSTS="*"

# Google Cloud Settings (Firestore, Cloud Run)
GOOGLE_CLOUD_PROJECT="your-google-cloud-project-id"
GOOGLE_CLOUD_LOCATION="us-central1"

# Database (PostgreSQL). Leave empty or remove to use SQLite locally.
# DATABASE_URL="postgres://user:password@localhost:5432/livecustomersupport"
